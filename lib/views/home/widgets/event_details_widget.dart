import 'package:honalondon_radio/consts/const.dart';

class EventsDetailsWidget extends StatelessWidget {
  const EventsDetailsWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text(events),
      ],
    );
  }
}
